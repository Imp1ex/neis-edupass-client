# 나이스플러스 클라이언트
다른 서비스에서 쓰려면 service_name이랑 secret_key_url 바꿔야 됨

src의 get_key_realtime.js를 실행하고 로그인 처리하면 aes키 획득 가능(`onepass987655432`) web 폴더는 원본 소스

예시로 봉사활동내역 api만 제공하고 나머지는 도와드릴 수 없습니다.

모든 요청 같은 session으로 요청하면 될 듯합니다.
